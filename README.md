# cisv-core

Core C library for CISV (SIMD CSV parser).

## Build

```bash
make -C core all
```

## Test

```bash
make -C core test
```

## Release policy

This repo publishes only core source/library artifacts.
Bindings are released independently in their own repos.
